import React from 'react';

function Home() {
    return(
    <div className="container">
        <body>
            <p>Need to build the Home website.</p>
        </body>
    </div>
    );
};

export default Home;